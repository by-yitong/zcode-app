name: zcode_app
description: ZCode AI 编程助手移动客户端
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  # 状态管理
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # 路由
  go_router: ^14.6.2

  # 网络
  dio: ^5.7.0
  web_socket_channel: ^3.0.1

  # 数据模型
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0

  # 本地存储
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.3

  # UI
  flutter_markdown: ^0.7.4+3

  # 工具
  uuid: ^4.5.1
  logger: ^2.5.0
  cached_network_image: ^3.4.1
  connectivity_plus: ^6.1.1
  flutter_animate: ^4.5.2
  crypto: ^3.0.7
  convert: ^3.1.2
  mobile_scanner: ^7.0.0
  highlight: ^0.7.0
  flutter_highlight: ^0.7.0
  image_picker: ^1.2.2
  file_picker: ^10.0.0
  share_plus: ^12.0.2
  url_launcher: ^6.3.1
  flutter_foreground_task: ^10.0.0
  flutter_local_notifications: ^22.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # 代码生成
  build_runner: ^2.4.14
  freezed: ^3.0.0
  json_serializable: ^6.9.0
  riverpod_generator: ^2.6.3
  custom_lint: ^0.7.3
  riverpod_lint: ^2.6.3
  flutter_launcher_icons: ^0.14.3

# 应用图标生成配置 (运行: dart run flutter_launcher_icons)
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/images/app_icon.png"
  # 自适应图标: 前景为图标图, 背景为品牌深色
  adaptive_icon_background: "#08090A"
  adaptive_icon_foreground: "assets/images/app_icon.png"
  min_sdk_android: 21
  remove_alpha_ios: true

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/

  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
    - family: JetBrains Mono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Regular.ttf
        - asset: assets/fonts/JetBrainsMono-Bold.ttf
          weight: 700

# flutter_markdown 已停更, vendor 进来打了崩溃补丁 (见 lib/src/builder.dart PATCH)
dependency_overrides:
  flutter_markdown:
    path: third_party/flutter_markdown
